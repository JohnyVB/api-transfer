const { Router } = require('express');
const { check } = require('express-validator');


const router = Router();

router.get('/');
router.post('/');
router.put('/');
router.patch('/');

module.exports = router;